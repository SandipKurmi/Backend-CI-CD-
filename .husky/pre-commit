# #!/usr/bin/env sh

# # Skip all checks if nothing is staged
# STAGED=$(git diff --cached --name-only)
# if [ -z "$STAGED" ]; then
#   exit 0
# fi

# npx lint-staged
# npm run test:ci
# npm run build
